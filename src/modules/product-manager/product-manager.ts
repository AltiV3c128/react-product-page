import { ServerManager } from '../server-manager/server-manager';
export class ProductManager {
    
    public static productList = ServerManager.getProductList();
    
    /* 
     * Other access methods to call for products features 
     */
}